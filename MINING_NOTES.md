# 因子挖掘 agentic 回路笔记（每轮 loop 由 Claude 读取/追加）

## Run 1 — 2026-06-10 02:1x（首次，默认配置）

- 面板: 50 标的 × 2513 日；pop150×gen25=2025 evals；horizon=5d
- 入库: `-1 * (open * ts_rank(open, 42))` 搜索|IC| .032 / ICIR 1.15 / 留出 .0315 ✅
- 观察:
  1. 只有 1 个因子过了贪心选择——redundancy/正适应度过滤偏严，
     大量候选 adj fitness ≤0 被剔
  2. 进化在 ~20 代收敛，后 5 代无改进 → 种群多样性不足
  3. 选出的因子用了 raw price level (open)，cs_zscore 部分消化了量纲，
     但混入了"价格水平"倾斜——下轮考虑对 leaf 做预标准化变体

## 下轮假设（agentic 提议，Run 2 执行）

- [ ] horizon=10/21 各跑一轮（不同预测期挖不同信号源）
- [ ] 种子补充: 量价相关 ts_corr 类（需新增算子）、缺口反转 (open/close[-1])、
      vol-adjusted momentum (mom/ts_std)
- [ ] redundancy 阈值 0.7→0.8，top_k 5→8，提高入库多样性
- [ ] seed 多换几个（7/13/99）对比挖掘稳定性

## Run 2 — 2026-06-10 02:2x（AST入库重挖 + 策略回测 + 预测上线）

- 同 seed 重挖结果一致（确定性 ✓），因子库含可执行 AST
- 横截面多空回测（±0.5美元中性，10只/边，5bp/边成本）:
  搜索期 Sharpe 1.37 / 年化13.7%；**留出2年 Sharpe 0.26 / 年化+3.6%（净成本为正）**
  → 真实但留出衰减 ~75%（典型 alpha decay），单因子不够，需要更多正交因子
- 预测端点 bug 修复：最新日横截面覆盖不足时回退到最近有效日
- 待查: 面板尾部覆盖只有 15/50（ts_rank 42d 窗口对数据缺口零容忍），
  考虑 rolling 改为允许 ≤10% NaN 或对缺口做前向填充
- 下轮: 执行 Run1 假设清单（horizon 10/21、新算子 ts_corr、多 seed、放宽冗余阈值）

## Run 3 — 2026-06-10 02:3x（多样化批次：h10/s13, h21/s99, h5/s7）

- 入库 +1: `abs(ts_z(ts_mean(high,5),63))/open` h21, 搜索IC .060/ICIR 1.23/留出 .084
  （"位移幅度溢价"+低价倾斜；h10 批次无入库）
- 双因子组合: 搜索 Sharpe 1.79/年化18.3% → **留出 1.57/年化19.8%**（单因子时 0.26）
- ⚠️ 诚实警示: 入库以"留出IC达标"为条件，多轮挖掘正在消耗留出集
  （selection-on-holdout 偏差）。1.57 偏乐观。对策:
  a) 从现在起冻结当前留出期结论，真验证 = 实时前向表现
  b) 后续挖掘改用"内层验证选择 + 留出只看一次"的嵌套划分
- 下轮: 组合接入每日预测/模拟盘做前向验证；嵌套划分改造
